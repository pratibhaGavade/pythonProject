# pythonProject
pythonProject

alway check 
        
        ls -a 
        
command and confirm you are in same folder of project

-----------
first run 

        python3 -m venv .venv

to create the .venv envirnoment folder so that the required softare  wont get install globaly

 
----------
after activate the .venv

        source .venv/bin/activate


-----------
Run the Install Command

Run this command to install the libraries listed in your requirements.txt:

        pip install -r requirements.txt


-----------------------
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
        